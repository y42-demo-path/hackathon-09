version: 2

seeds:
  - name: exchange_names_mapping
    config:
      y42:
        apiVersion: v1
    columns:
      - name: EXCHANGE_NAME_ORIGINAL
        data_type: TEXT
        tests:
          - unique
          - not_null
      - name: EXCHANGE_NAME_NEW
        data_type: TEXT
        tests:
          - not_null
          - unique
