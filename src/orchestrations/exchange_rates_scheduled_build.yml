version: 2

orchestrations:
  - name: exchange_rates_scheduled_build
    config:
      enabled: false
      command: y42 build -s +mrt_gaps_by_exchange_rate
      cron: 0 0/1 1/1 * *
      y42:
        apiVersion: v1
